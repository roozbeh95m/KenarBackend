{
  "routes": [
    {
      "method": "GET",
      "path": "/rates",
      "handler": "rates.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rates/count",
      "handler": "rates.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rates/:id",
      "handler": "rates.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/rates",
      "handler": "rates.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/rates/:id",
      "handler": "rates.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/rates/:id",
      "handler": "rates.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
